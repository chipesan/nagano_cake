<body>
    <header>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
      <%= link_to("Bookers", root_path, class: "navbar-brand") %>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav ml-auto">
          <% if user_signed_in? %>
          <li><%= link_to "商品一覧", user_path(current_user), class: "fas fa-home nav-link text-light" %>
         </li>
          <li><%= link_to "会員一覧", users_path, class: "fas fa-users nav-link text-light" %>
         </li>
         <li><%= link_to "注文履歴一覧", books_path, class: "fas fa-book-open nav-link text-light" %>
         </li>
         <li><%= link_to "ジャンル一覧", books_path, class: "fas fa-book-open nav-link text-light" %>
         </li>
          <li><%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: "fas fa-sign-out-alt nav-link text-light" %></li>
            <% else %>
        <li><%= link_to "Home", root_path, class: "fas fa-home nav-link text-light" %>
         </li>
          <li>
            <%= link_to "About", about_path, class: "fas fa-link nav-link text-light" %>
         </li>
          <li>
            <%= link_to "Sign up", new_user_registration_path, class: "fas fa-user-plus nav-link text-light" %>
     </li>
          <li>
            <%= link_to "Log in", new_user_session_path, class: "fas fa-sign-in-alt nav-link text-light" %>
        </li>
        <% end %>
        </ul>
      </div>
    </div>
  </nav>
</body>
</header>

<main>
<%= form_with model: @genre, url: admin_genres_create_path do |f| %>
  <h4>ジャンル名</h4>
  <%= f.text_field :name %>
  <%= f.submit "新規登録", class: "btn btn-success" %><br>
<% end %>

<th>ジャンル</th>
<table>
<% @genres.each do |genre| %>
<tr>
<td>
<%= genre.name %>
</td>
<td>
<%= link_to "Edit", admin_genres_edit_path(genre.id), class: "btn btn-sm btn-success" %>
</td>
</tr>
<% end %>
</table>
</main>