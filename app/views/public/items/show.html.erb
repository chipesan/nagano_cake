<br><th>ジャンル検索</th>
<table>
<% @genres.each do |genre| %>
<tr>
<td>
<%= genre.name %>
</td>
</tr>
<% end %>
</table><br>

<h2>商品詳細</h2>

    <p>画像</p>
     <% if @item.image.attached? %>
     <%= image_tag item.image, size: "150x100" %>
     <% else %>
     <%= image_tag 'no_image', size: "150x100" %>
     <% end %><br>

    <td>
      <h2><%= @item.name %></h2>
    </td>
    <td>
      <span><%= @item.introduction %></span>
    </td><br>
    <td>
    <td>
      <span><%= @item.price %></span>
    (税込)
    </td><br>
    <td>
    <%= form_with model: @cart_item, url: public_cart_items_create_path, method: :post do |f| %>
    <%= f.hidden_field :item_id, :value => @item.id %>
      <%= f.select :amount, [['1',1], ['2',2], ['3',3]], include_blank: "個数選択" %>
    </td>
    <td><%= f.submit 'カートに入れる' %></td>
    <% end %>