<%= form_with model: @address, url:public_addresses_update_path(@address.id), local: true do |f| %>
  <h4>配送先編集</h4>
  <%= f.text_field :postal_code %><br>
  <%= f.text_field :address %><br>
  <%= f.text_field :name %><br>
  <%= f.submit "変更内容を保存", class: "btn btn-success" %>
<% end %>